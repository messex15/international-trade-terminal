International Trade Terminal - Static Deployment Build

This folder is prebuilt and requires no npm/Vite build step.

Important: this prototype loads React and Lucide modules from esm.sh at runtime.

Keep confidential business data out until authentication and a secure backend are added.
